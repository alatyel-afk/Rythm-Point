/** Реэкспорт доменных типов протокола по сигналам (совместимость с импортом `./protocol-rules`). */
export type {
  LunchTemplateId,
  BreathingTemplateId,
  LoadTemplateId,
  SignalRuleId,
  Comparator,
  LunchItem,
  LunchTemplate,
  BreathingTemplate,
  LoadTemplate,
  BodySignals,
  DayContext,
  ConditionNode,
  SignalRule,
  ProtocolEngineInput,
  ProtocolEngineOutput,
  RuleEvaluationContext,
} from "./protocol-types";
