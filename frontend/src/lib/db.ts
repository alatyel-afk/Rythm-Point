/**
 * Database connection placeholder.
 * Currently uses in-memory store (see store.ts).
 * Replace with Prisma or other ORM when moving to PostgreSQL.
 */

export const DB_PROVIDER = "in-memory" as const;
