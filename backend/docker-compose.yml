services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: jyotish
      POSTGRES_PASSWORD: jyotish
      POSTGRES_DB: jyotish_protocol
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
